pipeline {
agent any
stages {
stage ('Build'){
steps { 
sh 'npm i'
sh 'ng build --prod'
 }
}
stage ('Dockerimage'){
steps {
sh 'docker build . -t myapp'
sh 'docker run -d --name myapp -p 8080:80 myapp'
 }
}
 }
 }
